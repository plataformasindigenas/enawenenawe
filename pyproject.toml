[project]
name = "enawenenawe"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "terradoc>=0.6.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
